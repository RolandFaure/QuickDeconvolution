#include <string>

std::string uniform_random_sequence(int length, char alphabet[], int sizeAlphabet);

void chromosomes_to_file(int length, char alphabet[], int sizeAlphabet, int nbChr, std::string fileOut);
